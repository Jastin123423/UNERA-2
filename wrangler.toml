@@ -1,20 +1 @@
# === Project Settings ===
name = "unera-2"                    # your Cloudflare Pages project name
compatibility_date = "2024-11-01"   # pick the date of your environment

# === D1 Database Binding ===
[[d1_databases]]
binding = "DB"                      # matches `env.DB` in your functions
database_name = "unera"             # your D1 database name
database_id = "fd656687-7a46-4250-849a-28eb60ea9173"  # your D1 ID

# === Durable Object (real-time feed) ===
[durable_objects]
bindings = [
  { name = "LIVE_FEED_DO", class_name = "LiveFeed" }  # DO class for live posts
]

# === Migrations (required for Durable Objects) ===
[[migrations]]
tag = "v1"
new_classes = ["LiveFeed"]
